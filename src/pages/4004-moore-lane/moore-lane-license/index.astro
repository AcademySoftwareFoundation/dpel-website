---
import LicenseLayout from "../../../layout/LicenseLayout.astro";
import MainLayout from "../../../layout/MainLayout.astro";

const licenseContent = await Astro.glob("../../../content/license/moore-lane-license.md");
---

<MainLayout title="Moore Lane License - DPEL">
  <div class="pb-32 container mx-auto">
    {
      licenseContent.map(license => (
        <LicenseLayout 
        frontmatter={license.frontmatter}
        url={license.url}
      />
      ))}
  </div>
</MainLayout>